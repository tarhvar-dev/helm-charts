{{- range .Values.sites }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .host | replace "." "-" }}
spec:
  secretName: {{ .host | replace "." "-" }}-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - {{ .host }}
---
{{- end }}
